# Aegis Suite — runtime dependencies
# Pinned to versions verified against Python 3.12 on 2025-01.
discord.py[voice]==2.7.1
davey>=0.0.1
fastapi==0.115.5
uvicorn[standard]==0.32.1
websockets==13.1
yt-dlp==2026.3.17
PyNaCl==1.5.0
pydantic==2.10.3
httpx==0.28.1

# Build-only (Windows EXE packaging). Listed here so CI test job has a complete env;
# Railway will install them but they are inert at runtime.
Pillow==11.0.0
pyinstaller==6.14.1

# Windows-only dependencies for desktop shortcut creation
pywin32==306; sys_platform == 'win32'
winshell==0.6; sys_platform == 'win32'

# Aegis Suite Rewrite — Phase 1 dependencies
sqlalchemy==2.0.31
alembic==1.13.1
hypothesis==6.102.2
pytest-asyncio==0.23.7
pytest==8.2.2
jinja2==3.1.6

